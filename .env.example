# XMTP Agent Configuration
# Copy this file to .env and fill in your values

# XMTP network: dev (testing) or production (live)
XMTP_ENV=dev

# Your ETH wallet private key (with 0x prefix)
# Jason: Export from your ETH CLI wallet
# Friends: Export from any ETH wallet (MetaMask, Foundry cast, etc.)
XMTP_WALLET_KEY=0x

# Encryption key for local XMTP database (64 hex chars = 32 bytes)
# Generate with: node -e "console.log('0x' + require('crypto').randomBytes(32).toString('hex'))"
XMTP_DB_ENCRYPTION_KEY=0x

# Local database path (relative to skill directory)
# ⚠️ CRITICAL: This directory MUST be persisted across restarts!
# Max 10 installations per inbox - deleting DB wastes quota.
XMTP_DB_PATH=./data/xmtp-db
