# sing-box 更新服务（Jason 🍎）

目标：一键把订阅源更新合并到 3 个模板配置中，并自动打上 **sing-box 1.14+ 兼容修复**。

## 你现在的三套配置（业务含义）
- **7.8 Air（分享版）**：可分享给朋友，只使用订阅源里的节点；不包含任何私有自建服务器。
- **5.9 Air（自用简化版）**：保持简化结构；需要保留你的私有服务器。
- **5.9 Pro（自用复杂版）**：包含 AI/流媒体/Apple 等分流；需要保留你的私有服务器。

## “方法 B”节点筛选规则（已确认）
更新时**只替换模板里已引用的节点 tag**（模板里有哪些具体节点 tag，就只更新这些 tag）。

优点：
- 不会误加你没在用的节点
- 规则稳定，不依赖地区关键词

## 兼容修复（固定执行，v2.1）
- `dns.servers` 迁移到新格式：`type/server`
- 删除 `dns_direct.detour=direct`（Android GUI 会报 *empty direct outbound*）
- 自动生成 **bootstrap DNS 白名单**：所有 outbounds.server 域名 → `dns_direct`
- 强制：`route.default_domain_resolver = dns_direct`（避免默认解析走 `dns_proxy` 造成全局断网）

> 注意：模板里的策略意图不动（例如你刻意加的 `cellular -> direct` 规则会保留）。

## 使用方式（手动/召唤）
在 OpenClaw 主对话里，你只需要说：

- “更新 sing-box 三版本”
- 或 “运行 singbox 更新服务”

我会按固定流程执行：
1) 下载订阅（你提供的 dler 链接）
2) 对 3 个模板分别合并更新
3) 输出 3 个带日期命名的 JSON，并发回 Telegram 附件

## 配置文件（已固定为“今天这三份模板”）
- `data/singbox_update_config.json`

## 脚本位置
- `scripts/singbox_update.py`

## 命令行（给 Jason 执行用）
```bash
python3 scripts/singbox_update.py \
  --url "<DLER_URL>" \
  --template-share sing-box_7.8-Air_share_YYYY-MM-DD_1.14-ready-v2.1.json \
  --template-air   sing-box_5.9-Air_private_YYYY-MM-DD_1.14-ready-v2.1.json \
  --template-pro   sing-box_5.9-Pro_private_YYYY-MM-DD_1.14-ready-v2.1.json \
  --outdir .
```

输出 3 行文件路径（每行一个）。
