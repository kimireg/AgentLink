{
  "name": "xmtp-agent-skill",
  "version": "2.0.0",
  "description": "XMTP decentralized messaging skill for OpenClaw agents (Agent SDK)",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22 <25"
  },
  "scripts": {
    "send": "node send.mjs",
    "listen": "node listener.mjs",
    "history": "node history.mjs",
    "check": "node check-new.mjs",
    "gen:keys": "node -e \"const c=require('crypto');console.log('XMTP_WALLET_KEY=0x'+c.randomBytes(32).toString('hex'));console.log('XMTP_DB_ENCRYPTION_KEY=0x'+c.randomBytes(32).toString('hex'));\""
  },
  "dependencies": {
    "@xmtp/agent-sdk": "^1.1.16"
  }
}
